import { FaSearch } from "react-icons/fa";

const Search = (props) => {
  return (
    <form className="searchBar" action="">
      <input type="search" placeholder="Search for a devleoper here..." />
    </form>
  );
};

export default Search;
